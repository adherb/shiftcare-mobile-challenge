import { Doctor, Slot } from '../types';

export function generateSlots(doctor: Doctor, date: Date): Slot[] {
  // TODO: Given a doctor and a date, generate 30-minute time slots
  // within their available hours for that day of the week.
  // Account for the doctor's timezone.
  throw new Error('Not implemented');
}
